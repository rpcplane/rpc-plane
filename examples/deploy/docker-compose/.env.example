# Copy to .env and fill in. Only set keys for the providers you actually use;
# unset ${VAR} references expand to an empty string (rpc-plane check warns about
# them). Never commit the real .env.
HELIUS_API_KEY=
QUICKNODE_API_KEY=
TRITON_API_KEY=
